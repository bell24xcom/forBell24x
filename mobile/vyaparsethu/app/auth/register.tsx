// Registration is the OTP login flow with a `role` param for post-verify
// routing — see the comment in ./login.tsx for why there's no separate
// registration endpoint.
export { default } from './login';
